import unittest


class TestAttachment(unittest.TestCase):

    def test_entry(self):



        pass
        # a = DocAttachment(attachment_uti, z_pk, attachment_identifier, note_pk, attachment_text, parent_pk,
        #                   generation=generation, url=url, blob_data=blob_data, draw_summary=draw_summary,
        #                   file_uuid=file_uuid, file_name=file_name)
        # a.represent = ""

        # print(a)

